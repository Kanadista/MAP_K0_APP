<?xml version="1.0" encoding="utf-8"?>


<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:elevation="10dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">


    <com.google.android.material.button.MaterialButton
        android:id="@+id/location_saved_button"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="20dp"
        android:layout_marginTop="10dp"
        app:icon="@drawable/ic_baseline_location_on_24"
        app:iconGravity="textStart"
        app:iconSize="75dp"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/location_recycler_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="prueba"
        app:layout_constraintLeft_toRightOf="@id/location_saved_button"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginStart="15dp"
        android:textSize="25sp"
        android:layout_marginTop="20dp" />

    <TextView
        android:id="@+id/location_recycler_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="prueba"
        android:layout_marginStart="20dp"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/location_recycler_title"
        app:layout_constraintLeft_toRightOf="@id/location_saved_button"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>